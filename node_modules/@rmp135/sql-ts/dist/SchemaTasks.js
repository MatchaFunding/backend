/**
 * Generates the schema name, removing invalid values.
 *
 * @export
 * @param {string} name Schema name.
 * @returns
 */
export function generateSchemaName(name) {
    if (name == null)
        return name;
    return name
        .replace(/\W/g, '')
        .replace(/^\d+/g, '');
}
//# sourceMappingURL=SchemaTasks.js.map